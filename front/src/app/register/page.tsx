import { Form } from "@/components/Form";

export default function Register() {

  return (
    <main className="body">
      <section className="section_login">   
        <Form login={false} />
      </section>
    </main>
  )
}
